{{#each locations}}

  <div class="card" style="width: 18.2rem;">
    <img src={{image}} class="card-img-top" alt="..." />
    <div class="card-body">
      <h5 class="card-title">{{city}}</h5>

      <a href="/location/{{id}}" class="btn btn-primary">Go somewhere</a>
    </div>
  </div>{{/each}}