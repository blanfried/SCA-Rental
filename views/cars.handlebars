<section class="container">
  <h3 class="car-name">{{car.name}}</h3>
  <h4 class="car-image">{{}}</h4>
  {{#if loggedIn}}
  <section class="flex-row justify-center">
    {{#each car.paintings as |painting|}}
    {{/each}}
  </section>
</section>
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}